const path = require('path');

module.exports = {
  config: path.resolve("./config", "database.js"), // creates database.js file
  "models-path": path.resolve("models"), // by default creates index file
  "seeders-path": path.resolve("database", "seeders"), // creates seeders folder
  "migrations-path": path.resolve("database", "migrations"), // creates migrations folder
};